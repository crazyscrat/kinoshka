import 'package:flutter/material.dart';

abstract class AppColors {
  static const authBtnNormal = Color.fromARGB(255, 1, 180, 228);
  static const authBtnOverlay = Color.fromARGB(255, 118, 118, 118);

  static const bottomBackgroundColor = Colors.black;
  static const bottomSelectedItemColor = Colors.white;
  static const bottomUnselectedItemColor = Colors.white54;

  static const textFieldBorderColor = Colors.grey;
  static const textFieldBorderFocusColor = Colors.grey;
  static const textFieldCursor = Colors.grey;

  static Color textFieldFillColor = Colors.white.withAlpha(235);
}